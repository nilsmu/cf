Module Main

    Sub Main()
        Dim a As New Arcade

        'a.checkPalindrome("aabaa")

        'a.maxMultiple(3, 10)

        'a.almostIncreasingSequence(New List(Of Integer)({1, 2, 3, 4, 3, 6}))

        'Dim list As New List(Of List(Of Integer))
        'list.Add(New List(Of Integer)({1, 1, 1, 0}))
        'list.Add(New List(Of Integer)({0, 5, 0, 1}))
        'list.Add(New List(Of Integer)({2, 1, 3, 10}))
        'Console.WriteLine(a.matrixElementsSum(list))
        'Console.ReadLine()

        'a.sortByHeight(New List(Of Integer)({-1, 150, 190, 170, -1, -1, 160, 180}))

        'a.reverseParentheses("test(str)ing")

        'a.alternatingSums(New List(Of Integer)({50, 60, 60, 45, 70}))

        'a.areSimilar(New List(Of Integer)({1, 2, 3}), New List(Of Integer)({2, 1, 3}))

        'a.palindromeRearranging("abaab")

        'a.killKthBit(1, 3)

        'a.arrayPacking(New List(Of Integer)({24, 85, 0}))

        'a.secondRightmostZeroBit(1073741824)

        a.swapAdjacentBits(74)

        Console.ReadKey()
    End Sub
End Module